import {
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
} from '@expo-google-fonts/inter'

/**
 * App typography — Inter, the workhorse of clean, premium app UI. The font is part of an app's
 * brand: to swap it per app, change the import + these two maps (e.g. to Plus Jakarta Sans, Geist,
 * Sora…). RN needs a distinct family per weight, so the Text primitive picks a family by variant.
 */
export const fontAssets = {
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
}

export const fonts = {
  regular: 'Inter_400Regular',
  medium: 'Inter_500Medium',
  semibold: 'Inter_600SemiBold',
  bold: 'Inter_700Bold',
}
