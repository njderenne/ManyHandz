node_modules
dist
.wrangler
*.local
.env
.env.*
!.env.example

# Worker local secrets (wrangler) — never commit
.dev.vars
.dev.vars.*
!.dev.vars.example

# Expo
.expo
expo-env.d.ts
web-build

# Native projects (generated by `expo prebuild` / EAS)
ios
android

# Metro / Expo Router generated types
.expo/types

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log
secrets.bulk.json

# iOS signing material written by builder/ios/bootstrap-credentials.js — NEVER commit
credentials.json
credentials/
*.p12
*.mobileprovision
*.key

# Readiness doctor output (regenerated each run)
SHIP_REPORT.md
